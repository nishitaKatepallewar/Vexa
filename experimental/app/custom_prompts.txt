Do NOT insert extra backslashes before spaces or to wrap lines. Only escape newlines as \n if needed. Never output Python with backslash line continuations.

Whenever doing location math in Blender, convert tuples to mathutils.Vector before subtraction or addition.

Only change shader values using
node.inputs["<Input>"].default_value = … (never direct attributes).
Use only real Blender input names (Noise: Scale/Detail/Roughness/Distortion, etc.).
Always create nodes with valid types and ensure material.use_nodes = True.

Use only Blender 4.x–valid properties.
Never access attributes unless they exist. Verify with hasattr(obj, "prop") before using any property.
For nodes, only modify values through: node.inputs["<Input>"].default_value = ….
